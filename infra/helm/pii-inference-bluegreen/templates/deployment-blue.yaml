apiVersion: apps/v1
kind: Deployment
metadata:
  name: pii-inference-blue
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: pii-inference
    app.kubernetes.io/instance: {{ .Release.Name }}
    slot: blue
spec:
  replicas: {{ .Values.blue.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: pii-inference
      slot: blue
  template:
    metadata:
      labels:
        app.kubernetes.io/name: pii-inference
        slot: blue
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      terminationGracePeriodSeconds: 60
      containers:
        - name: inference
          image: "{{ .Values.imageRegistry }}/{{ .Values.blue.image.repository }}:{{ .Values.blue.image.tag }}"
          imagePullPolicy: {{ .Values.blue.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          env:
            {{- range $key, $val := .Values.env }}
            - name: {{ $key }}
              value: {{ $val | quote }}
            {{- end }}
          envFrom:
            - secretRef:
                name: {{ .Values.secretName }}
          livenessProbe:
            {{- toYaml .Values.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.blue.resources | nindent 12 }}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: [ALL]
